// API de transcrição — extrai áudio do vídeo e transcreve usando Groq Whisper

import { NextResponse } from 'next/server';

export async function POST() {
  return NextResponse.json({ message: 'API de transcrição em construção' });
}
