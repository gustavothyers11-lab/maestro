// API de aulas — CRUD de aulas (GET listar, POST criar com upload de vídeo)

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'API de aulas em construção' });
}
